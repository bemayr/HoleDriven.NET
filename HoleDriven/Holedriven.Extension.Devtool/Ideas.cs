using HoleDriven;
using System;

namespace Holedriven.Extension.Devtool
{
    [Hole.Idea("provide a list of holes for the devtool: https://stackoverflow.com/questions/8621607/how-can-i-convert-a-list-of-filenames-to-a-tree-structure")]
    [Hole.Idea("create a qr code that can be scanned for mobile support")]
    [Hole.Idea("if connection does not work, make sure that devices are on the same network, maybe encode the network name in the url so that we can display it (make this configurable for security concerns)")]
    public class Ideas
    {

    }
}
